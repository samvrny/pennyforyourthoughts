<ol>
    {{#each posts as |post|}}
    <div class="container text-center">
        <div class="row justify-content-center">
            <li class="col-8">
                <article>
                    <h2>{{post.title}}</h2>
                    <p>{{post.post_contents}}</p>
                    <div>

                    </div>
                </article>
            </li>
        </div>
    </div>
        
    {{/each}}
</ol>